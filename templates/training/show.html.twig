{% extends 'base.html.twig' %}

{% block title %}Training{% endblock %}

{% block body %}
    <h1>Training</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ training.id }}</td>
            </tr>
            <tr>
                <th>NameTraining</th>
                <td>{{ training.nameTraining }}</td>
            </tr>
            <tr>
                <th>DescriptionTraining</th>
                <td>{{ training.descriptionTraining }}</td>
            </tr>
        </tbody>
    </table>
    <a class="btn btn-primary" href="{{ path('app_session_add', {'id': training.id}) }}">ajouter une nouvelle session</a>

    <a href="{{ path('app_training_index') }}"> Liste des formations</a>

    <a class="btn btn-primary" href="{{ path('app_training_edit', {'id': training.id}) }}">Editer la formation</a>

    {{ include('training/_delete_form.html.twig') }}
{% endblock %}
